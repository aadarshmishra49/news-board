<%= simple_form_for([@message, @message.comments.build]) do |f| %>
  <%= f.input :content, label: false, placeholder: "Comment", hide_label: true, input_html: { class: "col-12" } %>
  <%= f.button :submit, style: "background-color: #1798D1;" %>

<% end %>
