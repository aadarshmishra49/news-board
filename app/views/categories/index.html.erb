


<div class="sidebar">
<h3>
<strong> Categories</strong></h3>
<% @categories.each do |category| %>
  <div class="card text-white bg-red mb-3" style="max-width: 20rem;">
  <div class="card-header"><%= link_to category.name, category_path(category.id) %></div>
  </div>
<% end %>

</div>
